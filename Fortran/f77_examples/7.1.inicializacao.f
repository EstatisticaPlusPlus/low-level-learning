      program main
      IMPLICIT NONE
      real m, n, x, y

      ! Inicializando as variáveis
      data m /10/, n /20/, x /2.5/, y /2.5/

      ! Imprimindo as variáveis
      write (*,*) m
      write (*,*) n  
      write (*,*) x
      write (*,*) y

      STOP
      END
