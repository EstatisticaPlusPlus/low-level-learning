      program main
      IMPLICIT NONE
      real :: m, n, x, y  ! Declaração das variáveis

      ! Atribuindo valores às variáveis com a instrução DATA
      data m, n /10, 20/, x, y /2.5, 2.5/

      ! Imprimindo os valores das variáveis
      write (*,*) m
      write (*,*) n
      write (*,*) x
      write (*,*) y

      STOP
      END
